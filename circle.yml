general:
  branches:
    only:
      - f-circleci-testing

machine:
  environment:
    NODE_ENV: develop
    TZ: America/New_York
  node:
    version: 6.4

dependencies:
  cache_directories:
    - core-lib/node_modules

  override:
    - npm prune && npm install:
        pwd: core-lib
